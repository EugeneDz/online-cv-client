import React from 'react';
import { Layout } from 'antd';

const { Footer: AntFooter } = Layout;

const Footer = () => (
  <AntFooter style={{ textAlign: 'center' }}>UI povered by Ant Design</AntFooter>
);

export default Footer;
